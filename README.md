# loan-default-prediction
Loan Default Prediction | ML model to predict whether a borrower will repay or default on a LendingClub loan. Built with Random Forest + SMOTE for class imbalance + custom threshold optimization based on business cost. Deployed with Streamlit.
